- endpoint: "{{ prom_endpoint }}"
  metrics: ["{{ prom_metrics_profile_path }}"]
  token: "{{ prom_token }}"
  indexer:
    type: local
    createTarball: true
    tarballName: "{{ uuid }}-{{ env }}-{{ cluster }}"
    metricsDirectory: "{{ uuid }}-{{ env }}-{{ cluster }}"
