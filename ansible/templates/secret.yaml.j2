apiVersion: v1
kind: Secret
metadata:
  name: cherry-cloud-config
  namespace: kube-system
stringData:
  cloud-sa.json: |
    {
      "apiKey": "{{ vault_apiKey }}",
      "projectID": "{{ vault_projectID }}"
    }